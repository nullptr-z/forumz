package logic

import (
	"github.com/nullptr-z/forumz/entity"
)

func Register(user *entity.User) {

	// 1.判断用户是否存在
	// users := userQueryUserByName(user.Username)
	// 2.生成 ID
	// uid := snowflake.GetId()
	// 密码加密
	// 写入数据库
	// sqlx.
}
